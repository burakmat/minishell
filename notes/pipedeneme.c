#include <stdio.h>
#include <stdlib.h>
#include <string.h>
#include <sys/types.h>
#include <sys/wait.h>
#include <unistd.h>
 
int main()
{
	/*int fd[2];
	//fd[1] - write
	//fd[0] - read
	int pid;
	pid = fork()
	if (!pid)
	{
		if prev == 1
			exec()
	}
	else
	{
		waitpid(pid,0,0)
	}
	
	return 0;*/
}